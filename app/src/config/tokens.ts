import { NATIVE_MINT } from "@solana/spl-token";
import { PublicKey } from "@solana/web3.js";

export const NATIVE_TOKEN_ADDRESS = PublicKey.default;

export const WRAPPED_NATIVE_TOKEN_ADDRESS = NATIVE_MINT;
